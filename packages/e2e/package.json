{
  "name": "@meridian/e2e",
  "version": "0.1.0",
  "description": "Cross-stack convergence suite for Meridian: drives the full daily lifecycle (create → mint → trade → settle → redeem), all four trade paths, and a multi-user scenario against a real solana-test-validator using @meridian/sdk and @meridian/automation, re-verifying the on-chain invariants across the integrated stack.",
  "license": "ISC",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "ts-mocha -p tsconfig.json --timeout 600000 'tests/**/*.test.ts'"
  },
  "dependencies": {
    "@meridian/automation": "workspace:^",
    "@meridian/sdk": "workspace:^",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.98.0",
    "bn.js": "^5.2.1"
  },
  "peerDependencies": {
    "@pythnetwork/hermes-client": "^1.0.0",
    "@pythnetwork/pyth-solana-receiver": "^0.16.0"
  },
  "peerDependenciesMeta": {
    "@pythnetwork/hermes-client": {
      "optional": true
    },
    "@pythnetwork/pyth-solana-receiver": {
      "optional": true
    }
  },
  "devDependencies": {
    "@pythnetwork/hermes-client": "^1.0.0",
    "@pythnetwork/pyth-solana-receiver": "^0.16.0",
    "@types/bn.js": "^5.1.5",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^20.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
